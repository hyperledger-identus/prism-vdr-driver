# markdownlint configuration
# https://github.com/DavidAnson/markdownlint/blob/main/doc/Rules.md

default: true

# Allow long lines (common in tables, URLs, and generated content)
MD013: false

# Allow multiple top-level headings (common in multi-section docs)
MD025: false

# Allow inline HTML (needed for badges, details/summary, etc.)
MD033: false

# Allow duplicate headings in different sections
MD024:
  siblings_only: true

# Allow emphasis as heading (slide decks)
MD036: false

# Allow code block style mixing (fenced and indented)
MD046: false

# Allow bare URLs
MD034: false

# Allow non-sequential list numbering (slide decks continue across slides)
MD029: false
